<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.equestria.sosd_blog.Mapper.ArticleMapper">

    <insert id="postArticle" parameterType="com.equestria.sosd_blog.Domain.DTO.ArticlePostDTO" useGeneratedKeys="false">
        insert into articles
        (id, user_id, title, content_markdown, content_html, author, create_time, update_time)
        values(#{id},#{userId},#{title},#{contentText},#{contentHtml},#{author},NOW(),NOW())
    </insert>

</mapper>
